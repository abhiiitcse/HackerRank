#include <iostream>
#include <vector>
#include <algorithm>
#define FOR(i,a,b) for(int i=a;i<b;i++)
#define MAXN 100007
using namespace std;
typedef long long ll;
typedef vector<ll> vll;
vll edge[MAXN];

int main(){
  int tt;
return 0;
}
  
