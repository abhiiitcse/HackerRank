#include <bits/stdc++.h>

using namespace std;

long solve(long n){
    // Complete this function
}

int main() {
    int q;
    cin >> q;
    for(int a0 = 0; a0 < q; a0++){
        long n;
        cin >> n;
        long result = solve(n);
        cout << result << endl;
    }
    return 0;
}

