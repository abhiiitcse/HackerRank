#include <iostream>
#include <vector>
#include <algorithm>
#define FOR(i,a,b) for(int i=a;i<b;i++)
#define MAX_N 100005
using namespace std;
typedef long long ll;
ll a[MAX_N];
int main(){
  int n,k;
  cin>>n>>k;
  FOR(i,0,n){
    cin>>a[i];
  }
  return 0;
}
